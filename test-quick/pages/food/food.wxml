<view>
  <block wx:for="{{imgUrls}}" wx:key="*this">
  <view class="blockImage">
    <swiper-item>
      <image src="{{item}}" data-index='{{index}}' class="foodmenu" style="width:100%;" bindtap='previewImg' mode='widthFix'/>
    </swiper-item>
    </view>
  </block>

</view>