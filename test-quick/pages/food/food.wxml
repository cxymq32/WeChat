<view bindtap='sendMsg'>sssss</view>
<view>
  <block wx:for="{{imgUrls}}" wx:key="*this">
  <view class="blockImage">
    <swiper-item>
      <image src="{{item}}" data-index='{{index}}' class="foodmenu" style="width:100%;" bindtap='previewImg'/>
    </swiper-item>
    </view>
  </block>

</view>